{
  "healthchecks": {
    "web": [
      {
        "type": "startup",
        "name": "web check",
        "description": "Checking if the app responds to the /swagger-ui/index.html endpoint",
        "path": "/swagger-ui/index.html",
        "initialDelay": 30,
        "attempts": 5,
        "wait": 15
      }
    ]
  }
}
