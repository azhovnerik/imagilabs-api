# Welcome to the Colorful Matrix project!

# In this project, we will use:
# 1. the 2D matrix `m`, a grid where we can place data (like colors).
# 2. variables to set colors in our matrix `m`.

# Let's get started!

# First, we define our color variables using RGB color codes.
# These are in the format (R, G, B) where R = Red, G = Green, and B = Blue.
pink = (255, 170, 255)
blue = (100, 210, 240)
yellow = (250, 245, 120)
orange = (250, 165, 15)
green = (50, 225, 140)

# Now, let's color our matrix, `m`, using the colors we defined.

# Setting the top-left 2x2 square to yellow.
m[0][0] = yellow
m[0][1] = yellow
m[1][0] = yellow
m[1][1] = yellow

# Setting the top-center 2x2 square to green.
m[0][3] = green
m[0][4] = green
m[1][3] = green
m[1][4] = green

m[0][6] = blue
m[0][7] = blue
m[1][6] = blue
m[1][7] = blue

m[3][0] = blue
m[3][1] = blue
m[4][0] = blue
m[4][1] = blue

m[3][3] = pink
m[3][4] = pink
m[4][3] = pink
m[4][4] = pink

m[3][6] = orange
m[3][7] = orange
m[4][6] = orange
m[4][7] = orange


# --------------------------------------------------------------------------------
# More fun for the curious ones:

# Challenge:
# 1. Can you continue with the remaining 3 squares?
# 2. Can you create a new color variable and use it to color any 2x2 square?
# 3. Try swapping colors of any two 2x2 squares.
# Tip: Check out the Design tab for a color picker to help with the RGB color codes.

# Does the shape look familiar? It's the imagi logo!
# And the colors are imagi brand colors!

# The matrix m, index notation, variables, and the RGB color model are all
# introduced in-depth in the lesson materials and in the documentation.
